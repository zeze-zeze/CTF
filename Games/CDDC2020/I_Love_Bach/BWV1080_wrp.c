/*
 * This file belongs to an object file
 * generated by the Fugue compiler.
 */

#include <stdio.h>

extern void __cdecl fugue(
        /* pputchar's retuned value is ignored ! */
        int (__cdecl *pputchar)(int),
        int (__cdecl *pgetchar)(void));


/* In cygwin getchar() and putchar() do __NOT__ work!!
 * They are #defines there and not __cdecl functions! */
int __cdecl my_putchar(int c)
{
    return putchar(c);
}
int __cdecl my_getchar(void)
{
    return getchar();
}

int main(int argc,char **argv)
{
    fugue(my_putchar,my_getchar);
    fflush(stdout);
    return 0;
}
